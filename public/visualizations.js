(function(){
  const money=n=>new Intl.NumberFormat(undefined,{style:"currency",currency:"USD",maximumFractionDigits:2}).format(Number(n)||0);
  const esc=s=>String(s??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));
  const percent=n=>Math.max(0,Math.min(100,Number(n)||0));
  function rows(obj){const entries=Object.entries(obj||{}).sort((a,b)=>Number(b[1])-Number(a[1]));const max=Math.max(1,...entries.map(([,v])=>Number(v)||0));return entries.map(([k,v])=>`<div class="viz-row"><span>${esc(k)}</span><strong>${money(v)}</strong><div class="bar-track"><div class="bar-fill" style="width:${Math.max(4,(Number(v)||0)/max*100)}%"></div></div></div>`).join("")}
  function cards(items,empty){if(!items?.length)return `<div class="empty-viz">${esc(empty)}</div>`;return items.map(x=>`<div class="list-card"><strong>${esc(x.name||x.goal||"Item")}</strong><div class="mini-note">${x.due?esc(x.due)+" · ":""}${x.amount!=null?money(x.amount):""}</div></div>`).join("")}
  function take(text){return text?`<div class="fin-take"><strong>Fin's take ✦</strong><p>${esc(text)}</p></div>`:""}
  window.FinVisualizations={render(v){const d=v?.data||{};switch(v?.type){
    case"spending":return `<div class="big-number">${money(d.total)}</div><div class="mini-note">${Number(d.count)||0} purchases in the current Notion snapshot</div><div class="section-title">By category</div>${rows(d.categories)}<div class="stat-grid"><div class="stat"><div class="label">Needs</div><div class="value">${money(d.need)}</div></div><div class="stat"><div class="label">Wants</div><div class="value">${money(d.want)}</div></div><div class="stat"><div class="label">14-day avg/day</div><div class="value">${money(d.dailyAverage)}</div></div></div>${take(d.take)}`;
    case"bills":return `<div class="stat-grid"><div class="stat"><div class="label">Monthly bills</div><div class="value">${money(d.total)}</div></div><div class="stat"><div class="label">Still unpaid</div><div class="value">${money(d.unpaid)}</div></div></div><div class="section-title">Due next</div>${cards(d.soon,"Nothing due in the next 7 days.")}<div class="section-title">Overdue</div>${cards(d.overdue,"No overdue bills showing.")}${take(d.take)}`;
    case"savings":{const p=percent(d.percent_saved);return `<div class="big-number">${money(d.saved)} saved</div><div class="progress-track"><div class="progress-fill" style="width:${p}%"></div></div><div class="stat-grid"><div class="stat"><div class="label">Target</div><div class="value">${money(d.target)}</div></div><div class="stat"><div class="label">Remaining</div><div class="value">${money(d.remaining)}</div></div></div><div class="mini-note">${p}% of this goal is funded.</div>${take(d.take)}`}
    case"income":return `<div class="stat-grid"><div class="stat"><div class="label">This month</div><div class="value">${money(d.month)}</div></div><div class="stat"><div class="label">This week</div><div class="value">${money(d.week)}</div></div></div><div class="section-title">By source</div>${rows(d.sources)}${take(d.take)}`;
    case"room":return `<div class="big-number">${money(d.thisWeek)}</div><div class="mini-note">Estimated flexible room for the rest of this week</div><div class="stat-grid"><div class="stat"><div class="label">Per day</div><div class="value">${money(d.perDay)}</div></div><div class="stat"><div class="label">Month after bills</div><div class="value">${money(d.flexibleMonth)}</div></div></div><div class="section-title">Planned purchases</div>${cards(d.planned,"No upcoming planned purchases showing.")}${take(d.take)}`;
    default:return `<div class="empty-viz">I don't have a visualization for this answer yet.</div>`;
  }}};
})();
